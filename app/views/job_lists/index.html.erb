<p id="notice"><%= notice %></p>
<div class="container">
  <div class="pull-right">
<%= link_to 'New Job list', new_job_list_path, class: "btn btn-success" %>
  </div>
<h1 class="page-header">Forum Threads</h1>
      <% @job_lists.each do |job_list| %>
  <h3><%= link_to job_list.title, job_list %></h3>
    <% end %>
